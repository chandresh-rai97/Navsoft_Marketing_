node_modules
dist
dist-ssr
*.local
.DS_Store
.vercel
.netlify
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vite

# Local secret env files (the committed .env holds only the public publishable key)
.env.local
.env.*.local
